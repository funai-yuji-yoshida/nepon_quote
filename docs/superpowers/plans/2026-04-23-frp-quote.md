# FRP見積機能 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 既存のネポン見積ウィジェット（mitumori001）に、FRP製品専用の見積モードを追加する。

**Architecture:** ①基本情報タブにFRPボタンを追加し、押下すると `state.frpMode=true` に切り替わる。②見積明細タブでは既存の `sectionsContainer` を非表示にして `frpContainer` を表示する。FRPデータは `state.frpItems[]` で管理し、既存の `JSON` フィールドに追記保存する。PDFは既存の `pdf-generator.js` にFRP用ビルダー関数を追加する。

**Tech Stack:** 既存スタック（pdfmake、Zoho Embedded App SDK、vanilla JS）、追加ライブラリなし

---

## ファイル変更マップ

| ファイル | 変更種別 | 内容 |
|---|---|---|
| `app/index.html` | 修正 | FRPボタン追加（tab-basic）、frpContainer追加（tab-items） |
| `app/js/app.js` | 修正 | state追加、FRPモード関数群、save/load/updateOutput対応 |
| `app/js/pdf-generator.js` | 修正 | FRP用表紙行・明細ページビルダー関数追加 |
| `app/css/style.css` | 修正 | FRPコンテナ・トグル・テーブルのスタイル追加 |

---

## Task 1: HTML構造 — FRPボタン + frpContainer

**Files:**
- Modify: `mitumori001/app/index.html`

### FRPボタンの追加位置

`tab-basic` の `<h3 class="section-title">顧客・工事情報` の直前（`form-section` の末尾）に追加する。
具体的には `</div>` で閉じた後、新しい `form-section` として挿入する。

実際のHTMLでは「見積番号」セクションの `form-section` 末尾 `</div>` の直後、
「顧客・工事情報」の `<div class="form-section">` の直前に以下を挿入：

- [ ] **Step 1: FRPボタンをtab-basicに追加**

`app/index.html` の `<h3 class="section-title">顧客・工事情報` の直前に追加する：

```html
      <div class="form-section frp-mode-section">
        <h3 class="section-title">見積モード</h3>
        <div class="form-row">
          <button id="btnFrpMode" class="btn-frp-toggle" onclick="app.switchFrpMode()">FRP見積モードに切り替え</button>
          <span id="frpModeLabel" class="frp-mode-label" style="display:none">● FRPモード有効</span>
        </div>
      </div>
```

- [ ] **Step 2: frpContainerをtab-itemsに追加**

`app/index.html` の `<div id="sectionsContainer"` の直前に以下を挿入する：

```html
      <!-- FRP見積コンテナ（frpMode時のみ表示） -->
      <div id="frpContainer" style="display:none">
        <div class="frp-header">
          <div class="frp-ab-toggle">
            <span class="frp-ab-label">価格モード：</span>
            <button class="btn-ab active" id="btnFrpA" onclick="app.setFrpAB('A')">A価</button>
            <button class="btn-ab" id="btnFrpB" onclick="app.setFrpAB('B')">B価</button>
          </div>
          <button class="btn-frp-back" onclick="app.switchFrpMode()">← 通常モードに戻す</button>
        </div>

        <div class="frp-search-area">
          <input type="text" id="frpSearchInput" class="frp-search-input"
                 placeholder="品名・型式で検索..." autocomplete="off">
          <div id="frpSearchDropdown" class="frp-search-dropdown" style="display:none"></div>
        </div>

        <table class="frp-table">
          <thead>
            <tr>
              <th class="frp-col-no">番号</th>
              <th class="frp-col-name">品名</th>
              <th class="frp-col-qty">数量</th>
              <th class="frp-col-unit">単位</th>
              <th class="frp-col-price">定価単価</th>
              <th class="frp-col-total">定価合計</th>
              <th class="frp-col-shikiri">仕切単価</th>
              <th class="frp-col-shikiri-total">仕切合計</th>
              <th class="frp-col-del"></th>
            </tr>
          </thead>
          <tbody id="frpItemsTbody"></tbody>
        </table>

        <div class="frp-footer">
          <span class="frp-footer-label">定価合計：</span>
          <strong id="frpPriceTotal">¥0</strong>
          <span class="frp-footer-label" style="margin-left:16px;">仕切合計：</span>
          <strong id="frpShikiriTotal">¥0</strong>
        </div>
      </div>
```

- [ ] **Step 3: zet validate で構文確認**

```bash
cd mitumori001 && zet validate
```

期待結果: `Validation Rules passed successfully.`

- [ ] **Step 4: コミット**

```bash
git add mitumori001/app/index.html
git commit -m "feat: FRP見積 HTMLコンテナ追加"
```

---

## Task 2: CSSスタイル追加

**Files:**
- Modify: `mitumori001/app/css/style.css`

- [ ] **Step 1: style.cssの末尾に以下を追記**

```css
/* ── FRP見積モード ─────────────────────────────────── */

.frp-mode-section .btn-frp-toggle {
  background: #1a4d8f;
  color: #fff;
  border: none;
  border-radius: 6px;
  padding: 7px 18px;
  font-size: 13px;
  cursor: pointer;
}
.frp-mode-section .btn-frp-toggle.active {
  background: #c0392b;
}
.frp-mode-label {
  margin-left: 12px;
  color: #c0392b;
  font-weight: bold;
  font-size: 13px;
}

.frp-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 8px 12px;
  background: #eef3fa;
  border-radius: 6px 6px 0 0;
  border: 1px solid #c2d3ea;
  margin-bottom: 0;
}
.frp-ab-toggle {
  display: flex;
  align-items: center;
  gap: 6px;
}
.frp-ab-label {
  font-size: 12px;
  color: #555;
}
.btn-ab {
  padding: 4px 14px;
  border: 1px solid #aaa;
  border-radius: 4px;
  background: #fff;
  font-size: 13px;
  cursor: pointer;
}
.btn-ab.active {
  background: #1a4d8f;
  color: #fff;
  border-color: #1a4d8f;
}
.btn-frp-back {
  background: #fff;
  color: #555;
  border: 1px solid #aaa;
  border-radius: 4px;
  padding: 4px 12px;
  font-size: 12px;
  cursor: pointer;
}

.frp-search-area {
  position: relative;
  padding: 8px 12px;
  background: #f8fafd;
  border: 1px solid #c2d3ea;
  border-top: none;
}
.frp-search-input {
  width: 100%;
  padding: 6px 10px;
  border: 1px solid #bbb;
  border-radius: 4px;
  font-size: 13px;
  box-sizing: border-box;
}
.frp-search-dropdown {
  position: absolute;
  top: 100%;
  left: 12px;
  right: 12px;
  background: #fff;
  border: 1px solid #bbb;
  border-radius: 0 0 4px 4px;
  max-height: 200px;
  overflow-y: auto;
  z-index: 1000;
  box-shadow: 0 4px 8px rgba(0,0,0,0.12);
}
.frp-search-item {
  padding: 7px 12px;
  cursor: pointer;
  font-size: 13px;
  border-bottom: 1px solid #f0f0f0;
}
.frp-search-item:hover {
  background: #eef3fa;
}
.frp-search-item-name {
  font-weight: bold;
}
.frp-search-item-num {
  color: #666;
  font-size: 12px;
  margin-left: 8px;
}

.frp-table {
  width: 100%;
  border-collapse: collapse;
  border: 1px solid #c2d3ea;
  border-top: none;
  font-size: 13px;
}
.frp-table th {
  background: #dce8f7;
  padding: 6px 8px;
  text-align: center;
  font-weight: bold;
  border: 1px solid #b0c8e8;
  font-size: 12px;
}
.frp-table td {
  padding: 5px 8px;
  border: 1px solid #dde;
  vertical-align: top;
}
.frp-col-no    { width: 36px; text-align: center; }
.frp-col-qty   { width: 60px; }
.frp-col-unit  { width: 50px; text-align: center; }
.frp-col-price, .frp-col-total,
.frp-col-shikiri, .frp-col-shikiri-total { width: 90px; text-align: right; }
.frp-col-del   { width: 30px; text-align: center; }

.frp-item-name {
  font-weight: bold;
}
.frp-spec-row td {
  background: #fafafa;
  padding: 2px 8px 2px 24px;
  font-size: 12px;
  color: #555;
  border-top: none;
}
.frp-qty-input {
  width: 50px;
  border: 1px solid #bbb;
  border-radius: 3px;
  padding: 3px 4px;
  text-align: right;
  font-size: 13px;
}
.frp-footer {
  padding: 10px 14px;
  text-align: right;
  background: #f0f4fb;
  border: 1px solid #c2d3ea;
  border-top: none;
  border-radius: 0 0 6px 6px;
  font-size: 14px;
}
.frp-footer-label {
  color: #555;
  font-size: 12px;
}
```

- [ ] **Step 2: zet validate で確認**

```bash
cd mitumori001 && zet validate
```

- [ ] **Step 3: コミット**

```bash
git add mitumori001/app/css/style.css
git commit -m "feat: FRP見積 CSSスタイル追加"
```

---

## Task 3: stateにFRPフィールド追加 + モード切り替え関数

**Files:**
- Modify: `mitumori001/app/js/app.js`

- [ ] **Step 1: state オブジェクトにFRPフィールドを追記**

`app/js/app.js` の `state` オブジェクト（`shoka: '',` の行の直後）に以下を追加：

```javascript
    frpMode:     false,   // FRPモードフラグ
    frpAB:       'A',     // 'A' or 'B'
    frpItems:    [],      // FRP行リスト
    nextFrpId:   1,       // FRP行ID連番
```

- [ ] **Step 2: モード切り替え関数を追加**

`app.js` の `autoNumber` 関数の直前（採番系関数の近く）に以下を追加：

```javascript
  // ── FRP見積モード ──────────────────────────────────────────────

  function switchFrpMode() {
    const entering = !state.frpMode;
    const hasData  = entering
      ? (state.sections.length > 0 && state.sections.some(s => s.items.some(i => i.name)))
      : state.frpItems.length > 0;

    if (hasData) {
      const msg = entering
        ? '通常モードのデータが消えます。FRPモードに切り替えますか？'
        : 'FRPモードのデータが消えます。通常モードに戻しますか？';
      if (!confirm(msg)) return;
    }

    if (entering) {
      state.frpMode  = true;
      state.frpItems = [];
      state.nextFrpId = 1;
    } else {
      state.frpMode  = false;
      state.frpItems = [];
    }

    applyFrpModeUI();
    updateOutput();
  }

  function setFrpAB(ab) {
    state.frpAB = ab;
    document.getElementById('btnFrpA').classList.toggle('active', ab === 'A');
    document.getElementById('btnFrpB').classList.toggle('active', ab === 'B');
    renderFrpItems();
    updateFrpTotals();
    updateOutput();
  }

  function applyFrpModeUI() {
    const frpOn = state.frpMode;

    // tab-items の表示切り替え
    const frpContainer     = document.getElementById('frpContainer');
    const sectionsContainer = document.getElementById('sectionsContainer');
    const noSectionsMsg    = document.getElementById('noSectionsMsg');
    const itemsToolbar     = document.querySelector('#tab-items .items-toolbar');
    const gensuiSummary    = document.getElementById('gensuiSummary');

    if (frpContainer)      frpContainer.style.display      = frpOn ? '' : 'none';
    if (sectionsContainer) sectionsContainer.style.display = frpOn ? 'none' : '';
    if (noSectionsMsg)     noSectionsMsg.style.display     = frpOn ? 'none' : '';
    if (itemsToolbar)      itemsToolbar.style.display      = frpOn ? 'none' : '';
    if (gensuiSummary)     gensuiSummary.style.display     = 'none';

    // tab-basic のボタン状態
    const btn   = document.getElementById('btnFrpMode');
    const label = document.getElementById('frpModeLabel');
    if (btn) {
      btn.textContent = frpOn ? 'FRPモードを終了' : 'FRP見積モードに切り替え';
      btn.classList.toggle('active', frpOn);
    }
    if (label) label.style.display = frpOn ? '' : 'none';

    // A/B トグル同期
    if (frpOn) {
      document.getElementById('btnFrpA')?.classList.toggle('active', state.frpAB === 'A');
      document.getElementById('btnFrpB')?.classList.toggle('active', state.frpAB === 'B');
    }
  }
```

- [ ] **Step 3: public API に追記**

`app.js` の末尾付近の `return { ... }` ブロックに以下を追加：

```javascript
    switchFrpMode, setFrpAB,
```

- [ ] **Step 4: zet validate で確認**

```bash
cd mitumori001 && zet validate
```

- [ ] **Step 5: コミット**

```bash
git add mitumori001/app/js/app.js
git commit -m "feat: FRP stateフィールド・モード切り替え関数を追加"
```

---

## Task 4: FRP検索 + 行追加/削除

**Files:**
- Modify: `mitumori001/app/js/app.js`

- [ ] **Step 1: FRP検索・行追加関数を追加**

Task 3 で追加した `applyFrpModeUI` 関数の直後に以下を追加：

```javascript
  async function searchFrp(keyword) {
    if (!keyword || keyword.length < 1) {
      hideFrpDropdown();
      return;
    }
    try {
      let results = [];
      if (zohoReady) {
        const res = await ZOHO.CRM.API.searchRecord({
          Entity: 'FRP',
          Type: 'word',
          Query: keyword,
        });
        results = res?.data || [];
      }
      showFrpDropdown(results, keyword);
    } catch (e) {
      console.warn('FRP検索エラー:', e);
      hideFrpDropdown();
    }
  }

  function showFrpDropdown(results, keyword) {
    const dd = document.getElementById('frpSearchDropdown');
    if (!dd) return;
    if (results.length === 0) {
      dd.innerHTML = '<div class="frp-search-item" style="color:#888;">候補なし</div>';
    } else {
      dd.innerHTML = results.map((r, i) => `
        <div class="frp-search-item" data-idx="${i}">
          <span class="frp-search-item-name">${escHtml(r.Name || '')}</span>
          <span class="frp-search-item-num">${escHtml(r.itemnum || '')}</span>
        </div>
      `).join('');
      dd.querySelectorAll('.frp-search-item[data-idx]').forEach(el => {
        el.addEventListener('click', () => {
          const r = results[Number(el.dataset.idx)];
          addFrpItem(r);
          hideFrpDropdown();
          const inp = document.getElementById('frpSearchInput');
          if (inp) inp.value = '';
        });
      });
    }
    dd.style.display = '';
  }

  function hideFrpDropdown() {
    const dd = document.getElementById('frpSearchDropdown');
    if (dd) dd.style.display = 'none';
  }

  function addFrpItem(record) {
    const specs = [];
    for (let i = 1; i <= 9; i++) {
      const v = record[`spec${i}`] || '';
      if (v.trim()) specs.push(v.trim());
    }
    const item = {
      id:      state.nextFrpId++,
      frpId:   record.id || '',
      name:    record.Name    || '',
      itemnum: record.itemnum || '',
      qty:     1,
      unit:    record.unit    || '',
      price:   Number(record.price) || 0,
      priceA:  Number(record.A)     || 0,
      priceB:  Number(record.B)     || 0,
      specs,
    };
    state.frpItems.push(item);
    renderFrpItems();
    updateFrpTotals();
    updateOutput();
  }

  function removeFrpItem(itemId) {
    state.frpItems = state.frpItems.filter(i => i.id !== itemId);
    renderFrpItems();
    updateFrpTotals();
    updateOutput();
  }
```

- [ ] **Step 2: 検索入力イベントをinit()に追加**

`init()` 関数内（`buildExclusionUI()` の直後）に以下を追加：

```javascript
    // FRP検索入力イベント
    const frpInput = document.getElementById('frpSearchInput');
    if (frpInput) {
      let frpSearchTimer;
      frpInput.addEventListener('input', () => {
        clearTimeout(frpSearchTimer);
        frpSearchTimer = setTimeout(() => searchFrp(frpInput.value.trim()), 300);
      });
      frpInput.addEventListener('blur', () => {
        setTimeout(hideFrpDropdown, 200);
      });
    }
```

- [ ] **Step 3: public API に追記**

return ブロックに追加：

```javascript
    removeFrpItem,
```

- [ ] **Step 4: zet validate**

```bash
cd mitumori001 && zet validate
```

- [ ] **Step 5: コミット**

```bash
git add mitumori001/app/js/app.js
git commit -m "feat: FRP検索・行追加/削除関数を追加"
```

---

## Task 5: FRP描画 + 合計更新

**Files:**
- Modify: `mitumori001/app/js/app.js`

- [ ] **Step 1: renderFrpItems と updateFrpTotals を追加**

Task 4 の関数群の直後に追加：

```javascript
  function renderFrpItems() {
    const tbody = document.getElementById('frpItemsTbody');
    if (!tbody) return;

    const shikiriKey = state.frpAB === 'A' ? 'priceA' : 'priceB';
    const rows = [];

    state.frpItems.forEach((item, idx) => {
      const shikiri      = item[shikiriKey] || 0;
      const priceTotal   = item.price  * (Number(item.qty) || 1);
      const shikiriTotal = shikiri     * (Number(item.qty) || 1);
      const displayName  = `${escHtml(item.name)} ${escHtml(item.itemnum)}`.trim();

      rows.push(`
        <tr class="frp-item-row" data-frp-id="${item.id}">
          <td class="frp-col-no" style="text-align:center">${idx + 1}</td>
          <td class="frp-col-name frp-item-name">${displayName}</td>
          <td class="frp-col-qty">
            <input type="number" class="frp-qty-input" value="${item.qty}"
                   min="0" step="any" data-frp-id="${item.id}">
          </td>
          <td class="frp-col-unit" style="text-align:center">${escHtml(item.unit)}</td>
          <td class="frp-col-price">${fmt(item.price)}</td>
          <td class="frp-col-total">${fmt(priceTotal)}</td>
          <td class="frp-col-shikiri">${fmt(shikiri)}</td>
          <td class="frp-col-shikiri-total">${fmt(shikiriTotal)}</td>
          <td class="frp-col-del">
            <button onclick="app.removeFrpItem(${item.id})" style="color:#c00;background:none;border:none;cursor:pointer;font-size:14px;">✕</button>
          </td>
        </tr>
      `);

      item.specs.forEach(spec => {
        rows.push(`
          <tr class="frp-spec-row">
            <td></td>
            <td colspan="8" style="padding-left:24px;font-size:12px;color:#555;">${escHtml(spec)}</td>
          </tr>
        `);
      });
    });

    tbody.innerHTML = rows.join('');

    // 数量入力イベント
    tbody.querySelectorAll('.frp-qty-input').forEach(input => {
      input.addEventListener('input', () => {
        const id = Number(input.dataset.frpId);
        const item = state.frpItems.find(i => i.id === id);
        if (item) {
          item.qty = Number(input.value) || 0;
          renderFrpItems();
          updateFrpTotals();
          updateOutput();
        }
      });
    });
  }

  function updateFrpTotals() {
    const shikiriKey  = state.frpAB === 'A' ? 'priceA' : 'priceB';
    const priceTotal  = state.frpItems.reduce((s, i) => s + i.price  * (Number(i.qty) || 1), 0);
    const shikiriTotal = state.frpItems.reduce((s, i) => s + (i[shikiriKey] || 0) * (Number(i.qty) || 1), 0);

    const ptEl = document.getElementById('frpPriceTotal');
    const stEl = document.getElementById('frpShikiriTotal');
    if (ptEl) ptEl.textContent = '¥' + priceTotal.toLocaleString('ja-JP');
    if (stEl) stEl.textContent = '¥' + shikiriTotal.toLocaleString('ja-JP');
  }

  function fmt(n) {
    if (n === null || n === undefined || n === '') return '';
    const num = Number(n);
    if (isNaN(num) || num === 0) return '';
    return num.toLocaleString('ja-JP');
  }
```

> **注意:** `fmt` 関数は `pdf-generator.js` にも同名で存在するが、`app.js` 内のスコープ（IIFEクロージャ内）に同名のローカル `fmt` が既にない場合のみ追加する。既存の `fmt` があれば追加不要。

既存の `app.js` に `fmt` 関数があるか確認してから追加すること（`Grep` で `function fmt` を検索）。なければ上記を追加。

- [ ] **Step 2: public API に追記**

```javascript
    renderFrpItems, updateFrpTotals,
```

- [ ] **Step 3: zet validate**

```bash
cd mitumori001 && zet validate
```

- [ ] **Step 4: コミット**

```bash
git add mitumori001/app/js/app.js
git commit -m "feat: FRP描画・合計更新関数を追加"
```

---

## Task 6: updateOutput() のFRPモード対応

**Files:**
- Modify: `mitumori001/app/js/app.js`

- [ ] **Step 1: updateOutput() をFRPモード対応に修正**

`updateOutput()` 関数の冒頭（`readFormToState()` 呼び出しの直後、`calcGensui()` の前）に以下を追加：

```javascript
    // FRPモード時は専用合計を使用
    if (state.frpMode) {
      const shikiriKey   = state.frpAB === 'A' ? 'priceA' : 'priceB';
      const frpPriceTotal    = state.frpItems.reduce((s, i) => s + i.price * (Number(i.qty)||1), 0);
      const frpShikiriTotal  = state.frpItems.reduce((s, i) => s + (i[shikiriKey]||0) * (Number(i.qty)||1), 0);
      state.deliveryPrice    = frpShikiriTotal;
      state.dairiTotal       = frpShikiriTotal;

      setText('basicGrandTotal',   frpPriceTotal.toLocaleString('ja-JP'));
      setText('basicDairiTotal',   frpShikiriTotal.toLocaleString('ja-JP'));
      setText('basicDeliveryPrice', frpShikiriTotal.toLocaleString('ja-JP'));
      setText('basicGenkaTotal',   '0');
      setText('basicAraRi',        '0');
      setText('basicAraRiRate',    '―');

      const rowDairi = document.getElementById('rowDairiTotal');
      if (rowDairi) rowDairi.style.display = '';

      // ③ PDF出力タブのサマリーも更新
      const sections = state.sections;
      setText('sum-quoteNo',   state.seqNo || '（未採番）');
      setText('sum-date',      formatDisplayDate(new Date(getValue('quoteDate') || Date.now())));
      setText('sum-customer',  getValue('customerName') || '-');
      setText('sum-project',   getValue('projectName')  || '-');
      setText('sum-sections',  state.frpItems.length + '件');
      setText('sum-total',     '¥' + frpPriceTotal.toLocaleString('ja-JP'));
      setText('sum-dairi',     '¥' + frpShikiriTotal.toLocaleString('ja-JP'));
      setText('sum-discount',  '¥0');
      setText('sum-delivery',  '¥' + frpShikiriTotal.toLocaleString('ja-JP'));
      setText('sum-material',  '―');
      setText('sum-labor',     '―');
      setText('sum-welfare',   '―');
      setText('sum-anzen',     '―');
      const sumDairiRow = document.getElementById('sum-dairi-row');
      if (sumDairiRow) sumDairiRow.style.display = '';

      setText('grandTotalDisplay', '¥' + frpPriceTotal.toLocaleString('ja-JP'));
      updateQuoteNoBadge();
      return; // 通常の集計処理をスキップ
    }
```

- [ ] **Step 2: zet validate**

```bash
cd mitumori001 && zet validate
```

- [ ] **Step 3: コミット**

```bash
git add mitumori001/app/js/app.js
git commit -m "feat: updateOutput FRPモード対応"
```

---

## Task 7: 保存・読み込み対応

**Files:**
- Modify: `mitumori001/app/js/app.js`

- [ ] **Step 1: saveToCRM() にFRPデータを追加**

`saveToCRM()` の `jsonStr` 生成部分（`const jsonStr = JSON.stringify({` ブロック）に FRP フィールドを追記：

```javascript
      const jsonStr = JSON.stringify({
        sections:      state.sections,
        deliveryPrice: state.deliveryPrice,
        laborCost:     state.laborCost,
        seqNo:         state.seqNo,
        revision:      state.revision,
        exclusions:    state.exclusions,
        remarks:        state.remarks        || undefined,
        discount:       state.discount       || undefined,
        subformRowIds:  state.subformRowIds?.length ? state.subformRowIds : undefined,
        // FRP
        frpMode:       state.frpMode  || undefined,
        frpAB:         state.frpMode ? state.frpAB : undefined,
        frpItems:      state.frpMode && state.frpItems.length ? state.frpItems : undefined,
      });
```

- [ ] **Step 2: loadFromCRM() にFRP復元を追加**

`loadFromCRM()` の JSON パース後（`renumberSections()` の直前）に以下を追加：

```javascript
        // FRPモード復元
        if (parsed.frpMode) {
          state.frpMode    = true;
          state.frpAB      = parsed.frpAB || 'A';
          state.frpItems   = parsed.frpItems || [];
          state.nextFrpId  = state.frpItems.length > 0
            ? Math.max(...state.frpItems.map(i => i.id || 0)) + 1
            : 1;
        }
```

- [ ] **Step 3: applyStateToForm() の末尾にFRP UI適用を追加**

`applyStateToForm()` 関数の末尾に以下を追加：

```javascript
    // FRPモードUI適用
    if (state.frpMode) {
      applyFrpModeUI();
      renderFrpItems();
      updateFrpTotals();
    }
```

- [ ] **Step 4: zet validate**

```bash
cd mitumori001 && zet validate
```

- [ ] **Step 5: コミット**

```bash
git add mitumori001/app/js/app.js
git commit -m "feat: FRPデータの保存・読み込み対応"
```

---

## Task 8: buildPdfData() にFRPデータ追加

**Files:**
- Modify: `mitumori001/app/js/app.js`

- [ ] **Step 1: buildPdfData() にFRPフィールドを追記**

`buildPdfData()` の return ブロック末尾（`showUchiwake` の直後）に追加：

```javascript
      frpMode:   state.frpMode  || false,
      frpAB:     state.frpAB    || 'A',
      frpItems:  state.frpMode ? state.frpItems : undefined,
```

- [ ] **Step 2: zet validate**

```bash
cd mitumori001 && zet validate
```

- [ ] **Step 3: コミット**

```bash
git add mitumori001/app/js/app.js
git commit -m "feat: buildPdfData にFRPフィールドを追加"
```

---

## Task 9: PDF生成 — FRP対応

**Files:**
- Modify: `mitumori001/app/js/pdf-generator.js`

- [ ] **Step 1: FRP用合計計算を generate() に追加**

`pdf-generator.js` の `generate(data)` 関数内、`const sections = data.sections || [];` の直後に追加：

```javascript
    const frpMode  = data.frpMode  || false;
    const frpAB    = data.frpAB    || 'A';
    const frpItems = data.frpItems || [];

    // FRPモード用合計
    const frpPriceTotal   = frpItems.reduce((s, i) => s + (Number(i.price) || 0) * (Number(i.qty) || 1), 0);
    const frpShikiriTotal = frpItems.reduce((s, i) => {
      const p = frpAB === 'A' ? (Number(i.priceA) || 0) : (Number(i.priceB) || 0);
      return s + p * (Number(i.qty) || 1);
    }, 0);
```

- [ ] **Step 2: sectionTotals と grandTotal をFRPモードで差し替え**

`sectionTotals` と `grandTotal` の計算行を以下のように修正（既存の計算は else ブロックに移動）：

```javascript
    // セクション小計の計算
    let sectionTotals, grandTotal;
    if (frpMode) {
      // FRPモード: ダミーセクションとして扱う
      sectionTotals = [];
      grandTotal    = frpShikiriTotal;
    } else {
      sectionTotals = sections.map(s => {
        const subtotal = (s.items || []).reduce((sum, item) => sum + (Number(item.amount) || 0), 0);
        const secQty   = Math.max(1, Number(s.secQty) || 1);
        return { ...s, subtotal, secQty, effectiveTotal: subtotal * secQty };
      });
      grandTotal = sectionTotals.reduce((sum, s) => sum + s.effectiveTotal, 0);
    }
```

- [ ] **Step 3: buildCoverPage() にFRPモード表紙行を追加**

`buildCoverPage()` 関数内、`mirrorEntries.forEach(entry => {` ブロックの前に以下を追加：

```javascript
    // FRPモード: 表紙にFRP合計行を1行追加
    if (frpMode) {
      tableRows.push([
        { text: '1', alignment: 'center', fontSize: itemFs },
        { text: 'FRP機器一式', fontSize: itemFs },
        { text: '1', alignment: 'center', fontSize: itemFs },
        { text: '式', alignment: 'center', fontSize: itemFs },
        { text: '', fontSize: itemFs },
        { text: fmt(frpShikiriTotal), alignment: 'right', fontSize: itemFs },
      ]);
    }
```

ただし `frpMode`, `frpShikiriTotal`, `itemFs` は `buildCoverPage` の引数として渡す必要がある。
`buildCoverPage` の呼び出し側（`generate()` 内）を以下のように修正：

```javascript
        ...buildCoverPage({
          quoteNoStr, dateStr, branch,
          data, sectionTotals, grandTotal, discount, quoteCategory,
          deliveryPrice, materialCost, laborCost, legalWelfare, legalRate, anzenCost,
          mainRate: data.mainRate, pdfPriceMode: data.pdfPriceMode,
          showUchiwake,
          frpMode, frpItems, frpAB, frpPriceTotal, frpShikiriTotal,
        }),
```

`buildCoverPage` の引数リストに追加：

```javascript
  function buildCoverPage({ quoteNoStr, dateStr, branch, data, sectionTotals,
    grandTotal, discount, quoteCategory, deliveryPrice, materialCost, laborCost, legalWelfare, legalRate, anzenCost,
    mainRate, pdfPriceMode, showUchiwake,
    frpMode, frpItems, frpAB, frpPriceTotal, frpShikiriTotal }) {
```

`buildCoverPage` 内の `mirrorEntries.forEach` ループを、FRPモード時はスキップするよう修正：

```javascript
    if (!frpMode) {
      mirrorEntries.forEach(entry => {
        // ... 既存の forEach の中身をそのまま ...
      });
    } else {
      // FRPモード: 表紙に1行追加
      tableRows.push([
        { text: '1', alignment: 'center', fontSize: itemFs },
        { text: 'FRP機器一式', fontSize: itemFs },
        { text: '1', alignment: 'center', fontSize: itemFs },
        { text: '式', alignment: 'center', fontSize: itemFs },
        { text: '', fontSize: itemFs },
        { text: fmt(frpShikiriTotal), alignment: 'right', fontSize: itemFs },
      ]);
    }
```

- [ ] **Step 4: FRP用明細ページビルダー関数を追加**

`pdf-generator.js` の `buildDetailPages` 関数の直前に以下を追加：

```javascript
  function buildFrpDetailPages({ quoteNoStr, frpItems, frpAB, frpPriceTotal, frpShikiriTotal }) {
    const COL_WIDTHS = [22, '*', 25, 20, 45, 45, 50, 50];
    const shikiriLabel = `仕切単価(${frpAB}価)`;
    const shikiriTotalLabel = `仕切合計(${frpAB}価)`;

    const headerRow = [
      { text: 'No', style: 'tableHeader' },
      { text: '品名', style: 'tableHeader' },
      { text: '数量', style: 'tableHeader' },
      { text: '単位', style: 'tableHeader' },
      { text: '定価単価', style: 'tableHeader' },
      { text: '定価合計', style: 'tableHeader' },
      { text: shikiriLabel, style: 'tableHeader' },
      { text: shikiriTotalLabel, style: 'tableHeader' },
    ];

    const rows = [headerRow];
    let rowNo = 1;

    frpItems.forEach(item => {
      const shikiri      = frpAB === 'A' ? (item.priceA || 0) : (item.priceB || 0);
      const qty          = Number(item.qty) || 1;
      const priceTotal   = item.price * qty;
      const shikiriTotal = shikiri    * qty;
      const displayName  = `${item.name} ${item.itemnum}`.trim();

      rows.push([
        { text: String(rowNo++), alignment: 'center' },
        { text: displayName, bold: true },
        { text: String(qty), alignment: 'center' },
        { text: item.unit || '', alignment: 'center' },
        { text: fmt(item.price),   alignment: 'right' },
        { text: fmt(priceTotal),   alignment: 'right' },
        { text: fmt(shikiri),      alignment: 'right' },
        { text: fmt(shikiriTotal), alignment: 'right' },
      ]);

      item.specs.forEach(spec => {
        rows.push([
          { text: '', border: [true, false, false, false] },
          { text: `　${spec}`, fontSize: 8, color: '#555', colSpan: 7,
            border: [false, false, true, false] },
          {}, {}, {}, {}, {}, {},
        ]);
      });
    });

    // 空白行（最低2行）
    for (let i = 0; i < 2; i++) {
      rows.push([
        { text: '', border: [true, false, true, false] },
        { text: '', border: [false, false, false, false] },
        { text: '', border: [false, false, false, false] },
        { text: '', border: [false, false, false, false] },
        { text: '', border: [false, false, false, false] },
        { text: '', border: [false, false, false, false] },
        { text: '', border: [false, false, false, false] },
        { text: '', border: [false, false, true, false] },
      ]);
    }

    // 合計行
    rows.push([
      { text: '', border: [true, true, false, true], fillColor: '#e8f0f8' },
      { text: '合　　計', alignment: 'center', bold: true, colSpan: 4,
        border: [false, true, false, true], fillColor: '#e8f0f8' },
      {}, {}, {},
      { text: fmt(frpPriceTotal),   alignment: 'right', bold: true,
        border: [false, true, false, true], fillColor: '#e8f0f8' },
      { text: '', border: [false, true, false, true], fillColor: '#e8f0f8' },
      { text: fmt(frpShikiriTotal), alignment: 'right', bold: true,
        border: [false, true, true, true], fillColor: '#e8f0f8' },
    ]);

    return [{
      table: {
        widths:     COL_WIDTHS,
        headerRows: 1,
        body:       rows,
      },
      layout: {
        hLineWidth: (i, node) => (i === 0 || i === 1 || i === node.table.body.length) ? 0.8 : 0.3,
        vLineWidth: () => 0.5,
        paddingLeft:   () => 3,
        paddingRight:  () => 3,
        paddingTop:    () => 2,
        paddingBottom: () => 2,
        hLineColor: () => '#555',
        vLineColor: () => '#888',
      },
      margin: [0, 0, 0, 0],
    }];
  }
```

- [ ] **Step 5: generate() の明細ページ出力をFRPモード対応に修正**

`generate()` 内の `content:` ブロックの明細ページ部分を修正：

```javascript
        // FRPモード or 通常モード で明細ページを分岐
        ...(data.printDetail !== false ? [
          { text: '', pageBreak: 'after' },
          ...(frpMode
            ? buildFrpDetailPages({ quoteNoStr, frpItems, frpAB, frpPriceTotal, frpShikiriTotal })
            : buildDetailPages({ quoteNoStr, sectionTotals, grandTotal, mainRate: data.mainRate, pdfPriceMode: data.pdfPriceMode })
          ),
        ] : []),
```

- [ ] **Step 6: zet validate && zet pack**

```bash
cd mitumori001 && zet validate && zet pack
```

期待結果: `Extension packed successfully as mitumori001.zip in dist folder.`

- [ ] **Step 7: コミット**

```bash
git add mitumori001/app/js/pdf-generator.js mitumori001/dist/mitumori001.zip
git commit -m "feat: PDF生成 FRPモード対応（表紙・明細）"
```

---

## Task 10: 動作確認・最終ビルド

**Files:**
- Modify: `mitumori001/app/js/app.js`（バグ修正があれば）

- [ ] **Step 1: ローカルで動作確認**

以下の点を確認する（Zoho SDK未接続でもデモモードで確認可能）：

1. ①基本情報タブに「FRP見積モードに切り替え」ボタンが表示される
2. ボタン押下 → 確認ダイアログ → ②見積明細タブに切り替えてfrpContainerが表示される
3. 検索ボックスに文字入力 → （SDK接続時）候補ドロップダウンが表示される
4. 候補クリック → 行が追加され、仕様行がインデント表示される
5. 数量を変更 → 定価合計・仕切合計が即時更新される
6. A価/B価トグル → 仕切単価・仕切合計が切り替わる
7. 通常モードに戻す → 確認ダイアログ → 通常画面に戻る
8. 「保存」ボタン → CRM保存が実行される（SDK接続時）
9. 再ロード → FRPモードで復元される
10. PDF出力 → 表紙にFRP機器一式、明細にFRP列構成が出力される

- [ ] **Step 2: 最終ビルド**

```bash
cd mitumori001 && zet validate && zet pack
```

- [ ] **Step 3: 最終コミット**

```bash
git add mitumori001/dist/mitumori001.zip
git commit -m "build: FRP見積機能 最終ビルド"
```

---

## 実装上の注意事項

### app.js の public return ブロック
各タスクで追加した関数は必ず `return { ... }` ブロックに追加すること。HTMLの `onclick="app.xxx()"` で呼び出すために必要。

### fmt 関数の重複に注意
`app.js` の IIFE 内に既存の `fmt` 関数がない場合のみ Task 5 の fmt を追加する。事前に Grep で確認すること。

### applyStateToForm の確認
`applyStateToForm()` が存在する場合は末尾に FRP UI 適用コードを追加する（Task 7 Step 3）。関数が存在しない場合は `loadFromCRM()` の末尾に直接追加する。

### FRPモードでの印刷オプション
FRPモード時は常に明細ページを印刷する（`printDetail: true` を固定）。`buildPdfData()` の `printDetail` 計算部分でFRPモード時は `true` を返すよう修正が必要な場合は対応すること。
