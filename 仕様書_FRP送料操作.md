# FRPモード 送料操作 説明書

## 概要

FRPモードの送料には **2つの仕組み** があります。

| 仕組み | トリガー | 対象 |
|--------|----------|------|
| ① さいたま基準（自動） | FRPウィザードで製品を選択したとき | FRP製品に紐づく送料 |
| ② 手動選択（+ 送料ボタン） | 手動で `+ 送料` ボタンを押したとき | FRP製品選択以外の場合 |

---

## ① さいたま基準（FRP製品選択時の自動送料）

FRPウィザードで製品を選択すると、その製品の配送区分に応じて送料が自動的に処理されます。

### 混載便の場合

配送区分が **1〜9** の製品を選択すると、**送料行が自動追加**されます。操作は不要です。

```
製品を選択
  ↓
送料行が自動追加される（例：「送料　混載便／1台あたり」）
```

### チャーター便の場合

配送区分が **40・44・60・100** の製品を選択すると、**地域選択モーダルが自動表示**されます。

```
製品を選択
  ↓
「チャーター便【〇〇】地域選択」モーダルが表示される
  ↓
配送先の地域を選んで「選択」ボタンをクリック
  ↓
送料行が追加される（選択した地域の金額）
```

> **注意：** モーダルを × で閉じると送料行は追加されません。  
> その場合は製品行を削除して再度選択し直すか、価格を手動で入力してください。

### 配送区分の対応表

| 配送区分 | 種別 | 送料（さいたま基準） |
|---------|------|------------------|
| 1 | 混載便 | ¥11,000 |
| 2 | 混載便 | ¥17,000 |
| 3 | 混載便 | ¥22,000 |
| 4 | 混載便 | ¥28,000 |
| 5〜8 | 混載便 | — |
| 9 | 混載便 | ¥3,000 |
| 40 | チャーター便（4tユニック） | 地域選択 |
| 44 | チャーター便（4tユニック） | 地域選択 |
| 60 | チャーター便（6tユニック） | 地域選択 |
| 100 | チャーター便（10t平車） | 地域選択 |

---

## ② 手動選択（+ 送料ボタン）

FRP製品の選択とは関係なく、手動で送料行を追加したい場合に使用します。

```
「+ 送料」ボタンをクリック
  ↓
「送料を選択」ダイアログが表示される
  ↓
送料区分（混載・通常1 / 混載・中1 / 4t（平）・1 など）を選択
  ↓
送料行が追加される
```

送料区分・金額は所課ごとに CRM で管理されています。

---

## 送料行の編集・削除

### 金額を変更したい場合

追加された送料行の金額セルを直接クリックして編集できます。

### 削除したい場合

送料行右端の **× ボタン** をクリックして削除します。

### チャーター便の地域を選び直したい場合

1. 既存の送料行を × で削除
2. 製品行も × で削除
3. 再度 FRPウィザードで製品を選択
4. 地域選択モーダルで正しい地域を選択

---

## よくある質問

**Q. 製品を選択したのに送料が追加されなかった**  
A. チャーター便の場合、地域選択モーダルを × で閉じると送料は追加されません。製品を削除して再度選択してください。

**Q. 送料が自動追加されない製品がある**  
A. 配送区分が設定されていない製品は送料の自動追加が行われません。`+ 送料` ボタンから手動で追加してください。

**Q. 混載便と手動追加の送料が両方ある**  
A. 重複している場合は不要な行を × で削除してください。

**Q. 地域を選択しなくても送料が明細に追加される場合がある**  
A. 以下の2つのケースでは地域選択なしで送料が追加されます。

- **FRPウィザードで混載便製品（区分1〜9）を選択した場合** → 地域によらず金額が固定のため、自動的に送料行が追加されます。
- **`+ 送料`ボタンから手動で追加した場合** → CRMの送料マスターから選ぶだけで、地域選択は不要です。

地域選択が必要なのはチャーター便（区分40・44・60・100）のみです。
