# 切り上げルール（2026-07-23 改定）

## 適用条件

切り上げボタンをオンにした場合に適用されます。オフの場合は端数処理を行いません。

---

## ルール（FRPモード・通常モード共通）

| 価格帯 | 切り上げ単位 |
| ------ | ----------- |
| 100円未満 | そのまま（切り上げなし） |
| 100円以上1万円未満 | 10円単位 |
| 1万円以上100万円未満 | 100円単位 |
| 100万円以上 | 1,000円単位 |

FRPモード・通常モードで同一ルールを適用します。

適用箇所：

- FRPモード：仕切単価・仕切合計
- 通常モード：代理店単価

---

## 実装箇所

`mitumori001/app/js/app.js` — `roundUp` / `roundUpNormal`（同一ロジック）

```javascript
function roundUp(price) {
  if (!price || price < 100) return price;
  if (price < 10000)   return Math.ceil(price / 10)   * 10;
  if (price < 1000000) return Math.ceil(price / 100)  * 100;
  return                      Math.ceil(price / 1000) * 1000;
}
```
